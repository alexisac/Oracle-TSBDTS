Oracle password:
oracle

localhost:1521
localhost:1521/FREEPDB1


1. Am deschis SQL Plus

2. Am mutat sesiunea in PBD-ul FREEPBD1
ALTER SESSION SET CONTAINER = FREEPDB1;

3. Pentru a ma asigura ca am ajuns unde trebuie 
SHOW CON_NAME; // rezultatul trebuie sa fie FREEPBD1

4. Am creat userul aplicatiei:
CREATE USER vector_user IDENTIFIED BY vector_password
DEFAULT TABLESPACE USERS
TEMPORARY TABLESPACE TEMP
QUOTA UNLIMITED ON USERS;

5. Am acordat permisiuni pentru conectare si creare tabele
GRANT CONNECT, RESOURCE TO vector_user;
GRANT CREATE SESSION TO vector_user;
GRANT CREATE TABLE TO vector_user;

6. Dupa creare, m-am conectat cu acesta
CONNECT vector_user/vector_password@localhost:1521/FREEPDB1

7. Creez tabela code_chunks
CREATE TABLE code_chunks (
    id NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    file_path VARCHAR2(500) NOT NULL,
    package_name VARCHAR2(300),
    class_name VARCHAR2(300),
    symbol_name VARCHAR2(300) NOT NULL,
    chunk_type VARCHAR2(50) NOT NULL,
    source_code CLOB NOT NULL,
    embedding VECTOR
);

8. Rulez codul java ce populeaza tabela dar nu am embedding populat

9. Folosesc python sa rulez codul pentru embedding
In consola rulez urmatoarele:
pip install sentence-transformers
python -m pip install --upgrade torch
python main.py



